agents_matches_rivus=true
contributor_lock=- 同一项目的 `infer-std` 和 `infer-capsmap` 通过项目根目录下持久存在的 `.rivus-caps.lock` 串行化；进程内 registry 排斥同进程线程，POSIX record lock 排斥其他进程且不被 fork 后尚未 exec 的子进程继承
manual_lock=注意：推断基线始终包含分发 seed，再叠加项目 `caps/` 中除 `deps` 外的适用层，避免旧 deps 干扰重新推断。命令不会允许输出覆盖 active `caps/` 中的 `std`、`seed`、`suppress`、`ext` 或自定义层。首次运行时允许 `caps/` 不存在，使用分发 seed 推断并创建 `-o` 指定输出的父目录。同一项目的 `infer-std` 和 `infer-capsmap` 通过项目根目录下持久存在的 `.rivus-caps.lock` 串行化；进程内 registry 排斥同进程线程，POSIX record lock 排斥其他进程且不会被 fork 后尚未 exec 的子进程继承。
theory_lock=自动推断在 active `caps/` 目录中只能发布规范生成层：标准库推断写入 `std`，依赖推断写入 `deps`。自定义输出必须位于该目录之外，不能让自动生成内容借助自定义层的更高优先级覆盖人工 `ext` 修正。同一项目的 `infer-std` 和 `infer-capsmap` 通过项目根目录下持久存在的 `.rivus-caps.lock` 串行化；进程内 registry 排斥同进程线程，POSIX record lock 排斥其他进程且不会被 fork 后尚未 exec 的子进程继承。
